<Page x:Class="Madera_MMB.View_Crtl.Authentification"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
	Title="Authentification">
    
    
    <!-- Début Déclaration Template-->
    <Page.Resources>
        <BitmapImage x:Key="Logo" UriSource="../Lib/logo_madera.png"/>
    </Page.Resources>
        
    <Grid x:Name="MainGrid">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.25*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="0.25*" />
        </Grid.RowDefinitions>

        <Border Grid.Column="0" BorderBrush="Black" BorderThickness="2"/>
        <Border Grid.Column="1" BorderBrush="Black" BorderThickness="2"/>
        <Border Grid.Row="0" BorderBrush="Black" BorderThickness="2"/>

        <Border Grid.Column="2" BorderBrush="Black" BorderThickness="2"/>

        <Grid Grid.Column="0" Name="ImgContainer">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="25*" />
                <RowDefinition Height="75*" />
            </Grid.RowDefinitions>
            <Image Name="Logo" Grid.Column="0" Source="{StaticResource Logo}" Grid.Row="0" Width="{Binding ActualHeight, ElementName=rowDefinition0}" RenderTransformOrigin="0.706,0.609" Margin="10,10,10,10"/>
        </Grid>
        
        <!-- Début Déclaration Vue-->
        <Grid  Grid.Column="1" Background="YellowGreen" Width="{Binding ActualWidth, ElementName=columnDefinition1}" Height="{Binding ActualHeight, ElementName=rowDefinition0}">
            <Button Content="Valider" HorizontalAlignment="Left" VerticalAlignment="Top" Width="107" Margin="88,216,0,0" Height="32" Click="Button_Click"/>
            <Label Content="S'authentifier" HorizontalAlignment="Center" Height="28" Margin="117,33,95,0" VerticalAlignment="Top" Width="88"/>
            <TextBox HorizontalAlignment="Left" Height="42" Margin="88,81,0,0" TextWrapping="Wrap" Text="UserName" VerticalAlignment="Top" Width="133"/>
            <TextBox HorizontalAlignment="Left" Height="42" Margin="88,144,0,0" TextWrapping="Wrap" Text="Password" VerticalAlignment="Top" Width="133" TextChanged="TextBox_TextChanged"/>
       
        </Grid>
        <!-- Fin Déclaration Vue -->
        
    </Grid>
    <!-- Fin Déclaration Template -->

</Page>
