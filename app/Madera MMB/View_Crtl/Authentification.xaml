<Page x:Class="Madera_MMB.View_Crtl.Authentification"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 

      d:DesignHeight="1661.667" d:DesignWidth="2650.333"
	Title="Authentification">


    <!-- Début Déclaration Template-->
    <Page.Resources>
        <!-- Image Logo -->
        <BitmapImage x:Key="Logo" UriSource="../Lib/logo_madera.png"/>
        <!-- Label style -->
        <Style TargetType="{x:Type Label}">
            <Setter Property="FontSize" Value="12" />
        </Style>

        <Style TargetType="{x:Type Button}">
            <!-- Bouton style -->
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="bdr" BorderBrush="Black" BorderThickness="1 2 2 1" CornerRadius="2" Margin="3" Background="LightGray">
                            <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Center" ContentSource="Content" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bdr" Property="Background" Value="YellowGreen"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="{x:Type ToggleButton}">
            <!-- Toggle Bouton style -->
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ToggleButton}">
                        <Border x:Name="bdr" CornerRadius="90" Margin="3" Background="LightGray">
                            <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Center" ContentSource="Content" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="bdr" Property="Background" Value="YellowGreen" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid x:Name="MainGrid" Background="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.15*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="0.25*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="0.25*" />
        </Grid.RowDefinitions>


        <Grid x:Name="ImgContainer"  Grid.Row="0" Grid.Column="1">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

        </Grid>

        <Grid Grid.Row="1" Grid.Column="0" Grid.RowSpan="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="0.25*"/>
            </Grid.ColumnDefinitions>


        </Grid>

        <!-- Début Déclaration Vue-->
        <Grid Background="White"  Grid.Column="1" Grid.Row="0" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Width="{Binding ActualWidth, ElementName=columnDefinition1}" Height="{Binding ActualHeight, ElementName=rowDefinition1}" Grid.RowSpan="2" Grid.ColumnSpan="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="0.40*"/>
                <RowDefinition Height="0.40*"/>
                <RowDefinition Height="0.40*"/>
                <RowDefinition Height="0.40*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="0.25*"/>
                <ColumnDefinition Width="0.50*"/>
                <ColumnDefinition Width="0.40*"/>
            </Grid.ColumnDefinitions>

            <Image Name="Logo" Grid.Column="1" Source="{StaticResource Logo}" Grid.Row="0" Width="{Binding ActualHeight, ElementName=rowDefinition0}" RenderTransformOrigin="0.706,0.609" Margin="10,10,10,10"/>


            <Viewbox Margin="20,0,20,0" Grid.Row="1"  Height="50" Width="250" Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center">
                <Label Content="Nom d'utilisateur : " />
            </Viewbox>
            <Viewbox Margin="20,0,20,20" Grid.Row="2" Grid.Column="1" Height="45" Width="250" HorizontalAlignment="Center" VerticalAlignment="Center" >
                <TextBox x:Name="username"  Height="45" Width="250" TextWrapping="Wrap"/>
            </Viewbox>

            <Viewbox Margin="20,0,20,0" Grid.Row="3"  Height="50" Width="250" Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center">
                <Label Content="Mot de passe : " />
            </Viewbox>
            <Viewbox Margin="20,0,20,0" Grid.Row="4"  Height="45" Width="250" Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center">
                <PasswordBox x:Name="password" Height="45" Width="250" />
            </Viewbox>

            <Viewbox Grid.Row="5" Grid.Column="1" Height="100" Width="250" HorizontalAlignment="Right" VerticalAlignment="Center" >
                <Button x:Name="validAuth" Content="Valider" FontSize="15" Height="50" Width="250" />
            </Viewbox>

            <Viewbox Grid.Row="5" Grid.Column="1" Height="100" Width="250" HorizontalAlignment="Left" VerticalAlignment="Center" >
                <Button x:Name="Leave" Content="Quitter" FontSize="15" Height="50" Width="250" Click="Quitter"/>
            </Viewbox>
        </Grid>
        <!-- Fin Déclaration Vue -->


    </Grid>
    <!-- Fin Déclaration Template -->

</Page>
